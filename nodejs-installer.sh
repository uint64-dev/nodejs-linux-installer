clear
echo "This installer comes with both Node.JS and NPM (Node Package Manager). The installation should take no more than 10 minutes. Click any key to proceed with the instllation."
read input
sudo apt install nodejs npm